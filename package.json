{
  "name": "odin-agent",
  "version": "0.0.1",
  "private": true,
  "description": "Open command portal.",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.10.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
