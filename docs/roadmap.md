# Roadmap

Sketch, not a commitment.

## Want first

- A small SDK that wraps the agent HTTP API.
- A Fastify service that owns the OpenAI key.
- A CLI that talks to the service.

## Maybe

- On-chain registry for agent manifests.
- Persistent threads.
- Tool bridge.
