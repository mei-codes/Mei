{
  "name": "@odin/server",
  "version": "0.0.1",
  "private": true,
  "description": "Fastify server exposing /api/health and /api/agent.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "odin-server": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@odin/sdk": "workspace:*",
    "fastify": "^5.0.0",
    "@fastify/cors": "^10.0.1",
    "openai": "^4.67.0",
    "pino": "^9.4.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.4"
  }
}
